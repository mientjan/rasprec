# Install at /etc/systemd/system/docker.service.d/rasprec-mount.conf.
# This is a dedicated recorder host: Docker must never start without its data disk.
[Unit]
RequiresMountsFor=/srv/rasprec
AssertPathIsMountPoint=/srv/rasprec
